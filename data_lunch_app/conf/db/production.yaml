name: lunch
url: sqlite:///shared_data/${db.name}.db
engine:
  _target_: sqlalchemy.create_engine
  url: ${db.url}
menu_table: menu
orders_query: "
  SELECT orders.id, orders.user, item
  FROM orders
  LEFT JOIN menu
  ON menu.id = orders.menu_item_id ;
  "
